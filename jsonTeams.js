/*const listado = document.querySelector("#listado");

const constJson = async () => {
    const resp = await fetch("./teams.json");
    const data = await resp.json();

    data.forEach((post)=> {
        const li = document.createElement("li");
        li.innerHTML = `
                <h2 class="paises">${post.pais}</h2>

        `;





        listado.append(li);
    });
};

constJson();
*/